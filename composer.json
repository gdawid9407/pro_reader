{
    "name": "yourname/reader-engagement-pro",
    "description": "WordPress plugin for tracking reading progress",
    "type": "wordpress-plugin",
    "require": {
        "php": ">=7.4",
        "composer/installers": "^1.10"
    },
    "autoload": {
        "psr-4": {
            "ReaderEngagementPro\\": "includes/"
        }
    },
    "config": {
        "allow-plugins": {
            "composer/installers": true
        }
    }
}
